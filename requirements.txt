# ATS Resume Scorer — Backend Python dependencies
#
# Install with:
#     pip install -r requirements.txt
#
# After installing, download the spaCy model:
#     python -m spacy download en_core_web_md
#

# ── Web framework ──
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
python-multipart>=0.0.9      # required by FastAPI for File/Form uploads
python-dotenv>=1.0

# ── NLP / ML ──
spacy>=3.7,<4.0
sentence-transformers>=2.5
numpy>=1.26
rapidfuzz>=3.6
torch>=2.0                   # required by sentence-transformers
transformers>=4.30            # required by sentence-transformers

# ── File parsing ──
pdfplumber>=0.11
PyPDF2>=3.0
python-docx>=1.1

# ── PDF report generation ──
jinja2>=3.1
reportlab>=4.0.0             # PDF generator engine
weasyprint>=60.0             # HTML-to-PDF (needs libcairo2 on Linux)

# ── External services ──
httpx>=0.27                  # async Supabase REST client
groq>=0.13                   # Groq LLM client (LLaMA 3.3)
PyJWT[crypto]>=2.8           # verifies Supabase access tokens
